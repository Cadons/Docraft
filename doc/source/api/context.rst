Document Context
================

Shared rendering and layout state used across the entire document pipeline.
Holds the active backend, cached sub-backends (text, shape, image, page),
page metrics, section nodes, and the layout cursor.

.. doxygenclass:: docraft::DocraftDocumentContext
   :project: docraft
   :members:
